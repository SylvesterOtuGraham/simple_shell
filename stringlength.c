#include "shell.h"
/**
 * stringlen - length of a string
 * @s: string
 * Return: length
 */
int stringlen(char *s)
{
int length;
for (length = 0; s[length] != '\0'; length++)
{

}
return (length);
}

